import 'package:flutter/material.dart';

class AppSvgIcon extends StatelessWidget {
  const AppSvgIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
